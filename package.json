x{
  "name": "sample-node-app-ih",
  "version": "1.0.0",
  "author": "Your Name",
  "license": "MIT",
  "engines": { "node": ">=14.0.0" },

  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "test:ci": "jest --ci --reporters=default --reporters=jest-junit --coverage --coverageReporters=lcov,text-summary --testLocationInResults"
  },

  "dependencies": {
    "express": "^4.18.2"
  },

  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "jest-environment-jsdom": "^29.7.0",
    "supertest": "^6.3.3",
    "@testing-library/jest-dom": "^6.1.4",
    "jsdom": "^23.0.1"
  },

  "jest-junit": {
    "outputDirectory": "coverage",
    "outputName": "junit.xml"
  }
}
